Area calculation using with arguments with return value method.
#include <stdio.h>
int area (int a,int b){
    int r=a*b;
    return r;
}
int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    int k=area(a,b);
    printf("%d",k);
    return 0;
}

output:
1
2
2
