Read a year and print year is leap year or not

#include <stdio.h>

int main()
{
    int y;
    scanf("%d",&y);
    if(y%4==0 && (y%100!=0 || y%400==0)){
        printf("Leap year\n");
    }
    else{
        printf("Non leap year");
    }
    return 0;
}

output:
2100
Non leap year
