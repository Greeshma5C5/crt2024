Factorial of a given no using Recursion
#include <stdio.h>
int fact(int n){
    if(n==0)
       return 1;
    return n*fact(n-1);
}
int main() {
    int n;
    scanf("%d",&n);
    int r=fact(n);
    printf("%d",r);
    return 0;
}
output:
5
120
