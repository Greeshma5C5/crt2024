Division of 2 numbers using if condition

#include <stdio.h>

int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    if (b==0)
    {
       printf("not possible\n");
       b=1;
    }
    int x=a/b;
    printf("%d",x);
    return 0;
}



output:
12
0
not possible
12
