Sum of N natural nos using Recursion
#include <stdio.h>
int add(int n){
    if(n==0)
       return 0;
    return n+add(n-1);
}
int main() {
    int n;
    scanf("%d",&n);
    int r=add(n);
    printf("%d",r);
    return 0;
}
output:
5
15
