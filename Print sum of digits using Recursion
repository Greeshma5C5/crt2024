Print sum of digits using Recursion
#include <stdio.h>
int sumofdigits(int n){
    if(n==0)
       return 0;
    return n%10+sumofdigits(n/10);
}
int main() {
    int n;
    scanf("%d",&n);
    int r=sumofdigits(n);
    printf("%d",r);
    return 0;
}
output:
186
15
