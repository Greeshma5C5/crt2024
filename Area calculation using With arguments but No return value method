Area calculation using With arguments but No return value method
#include <stdio.h>
int area (int a,int b){
    int r=a*b;
    printf("%d",r);
}
int main() {
    int a,b;
    scanf("%d%d",&a,&b);
    area(a,b);
    return 0;
}

output:
1 2 
2
