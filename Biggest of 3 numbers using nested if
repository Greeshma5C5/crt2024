Read 3 numbers anf find greatest of 3 numbers using nested if.

#include <stdio.h>

int main()
{
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    if(a>b){
        if(a>c){
        printf("%d",a);
        }
        else{
            printf("%d",c);
        }
    }
    else
    if(b>c){
          printf("%d",b);
       }
    else{
           printf("%d",c);
      } 
    return 0;
}

output:
1
2
3
3
